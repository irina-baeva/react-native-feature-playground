import React from "react";
import { View, StyleSheet, Text } from "react-native";

const PlacedatailsScreen = () => {
	return <Text>Acoount screen</Text>;
};

const styles = StyleSheet.create({});

export default PlacedatailsScreen